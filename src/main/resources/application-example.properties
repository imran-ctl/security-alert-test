spring.application.name=security-alert-test
server.port=8080
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
# GitHub configuration — set the GITHUB_TOKEN environment variable before running
github.token=${GITHUB_TOKEN}
