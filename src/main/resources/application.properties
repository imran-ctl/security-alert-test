spring.application.name=security-alert-test
server.port=8080
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
# GitHub configuration — token is read from the GITHUB_TOKEN environment variable
github.token=${GITHUB_TOKEN}
